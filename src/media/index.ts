// MEDIA — shared URL/object resolution for CDN-hosted assets (avatars today;
// thumbnails/previews can join here). Platform-free; web + mobile import these so
// the deterministic object-key conventions live in ONE place.
export * from './avatar';
