// @paxpia/core/social — the persistent social-graph domain (video comments today;
// follows/likes listings can join later). Platform-free types + pure logic; the fetch
// impl lives per-platform behind `CommentsApi`. See ./comments.ts.
export * from './comments';
